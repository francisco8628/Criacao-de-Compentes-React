import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(//o que vai ser renderizad 
    <App />,
  document.getElementById('root')//(onde vai ser renderizado) vai buscar a div com este id
);

